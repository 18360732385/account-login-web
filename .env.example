# 浏览器侧 API 基址：
# - 留空或设为 '' → 使用相对路径 /api/...，由 Vite 开发代理转发到后端
# - 设为 http://localhost:8080 → 直连后端（需后端 CORS 放行）
VITE_API_BASE_URL=

# 仅 Vite 开发服务器代理目标（不暴露给浏览器包）
VITE_API_PROXY_TARGET=http://localhost:8080
