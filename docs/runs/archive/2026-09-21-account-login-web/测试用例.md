# 测试用例 — 账号登录前端（F）

| ID | 场景 | 步骤 | 期望 | 类型 |
|---|---|---|---|---|
| TC01 | 登录成功（API） | mock fetch 200 + token | login() 返回 accessToken | 单元 |
| TC02 | 登录失败（API） | mock 401 + message | 抛 ApiError 401 | 单元 |
| TC03 | /me 已认证（API） | mock GET 200 | fetchMe 返回 username/displayName | 单元 |
| TC04 | /me 未授权（API） | mock 401 | 抛 ApiError | 单元 |
| TC05 | App 登录成功流 | 填 demo 凭据 → 点登录 | 显示资料 + Token 截断 | 组件集成 |
| TC06 | App 登录失败流 | 错误密码 → 点登录 | role=alert 展示错误；仍在表单 | 组件集成 |
| TC07 | API 基址默认 | getApiBaseUrl() | 空串（代理模式） | 单元 |

映射：`src/api/client.test.ts` + `src/App.test.tsx`。
